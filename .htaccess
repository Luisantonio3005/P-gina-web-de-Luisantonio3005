# Configuración para GitHub Pages y optimización de recursos
# Luis Antonio Canales Guerrero - Página Web

# Habilitar compresión GZIP
<IfModule mod_deflate.c>
    AddOutputFilterByType DEFLATE text/plain
    AddOutputFilterByType DEFLATE text/html
    AddOutputFilterByType DEFLATE text/xml
    AddOutputFilterByType DEFLATE text/css
    AddOutputFilterByType DEFLATE application/xml
    AddOutputFilterByType DEFLATE application/xhtml+xml
    AddOutputFilterByType DEFLATE application/rss+xml
    AddOutputFilterByType DEFLATE application/javascript
    AddOutputFilterByType DEFLATE application/x-javascript
</IfModule>

# Headers de caché para recursos estáticos
<IfModule mod_expires.c>
    ExpiresActive On
    
    # CSS y JavaScript - 1 año
    ExpiresByType text/css "access plus 1 year"
    ExpiresByType application/javascript "access plus 1 year"
    ExpiresByType text/javascript "access plus 1 year"
    
    # Imágenes - 1 año
    ExpiresByType image/png "access plus 1 year"
    ExpiresByType image/jpg "access plus 1 year"
    ExpiresByType image/jpeg "access plus 1 year"
    ExpiresByType image/gif "access plus 1 year"
    ExpiresByType image/webp "access plus 1 year"
    ExpiresByType image/svg+xml "access plus 1 year"
    
    # HTML - 1 hora
    ExpiresByType text/html "access plus 1 hour"
    
    # XML - 1 día
    ExpiresByType application/xml "access plus 1 day"
    ExpiresByType text/xml "access plus 1 day"
</IfModule>

# Headers de seguridad y optimización
<IfModule mod_headers.c>
    # Permitir acceso desde cualquier origen para recursos estáticos
    <FilesMatch "\.(css|js|png|jpg|jpeg|gif|webp|svg|ico)$">
        Header set Access-Control-Allow-Origin "*"
        Header set Cache-Control "public, max-age=31536000"
    </FilesMatch>
    
    # Headers para HTML
    <FilesMatch "\.(html|htm)$">
        Header set Cache-Control "public, max-age=3600"
        Header set X-Content-Type-Options "nosniff"
        Header set X-Frame-Options "SAMEORIGIN"
        Header set X-XSS-Protection "1; mode=block"
    </FilesMatch>
    
    # Headers para XML (sitemaps)
    <FilesMatch "\.(xml)$">
        Header set Content-Type "application/xml; charset=utf-8"
        Header set Cache-Control "public, max-age=86400"
    </FilesMatch>
</IfModule>

# Redirecciones y configuración de URL
<IfModule mod_rewrite.c>
    RewriteEngine On
    
    # Forzar HTTPS (si está disponible)
    # RewriteCond %{HTTPS} off
    # RewriteRule ^(.*)$ https://%{HTTP_HOST}%{REQUEST_URI} [L,R=301]
    
    # Redirigir www a no-www (opcional)
    # RewriteCond %{HTTP_HOST} ^www\.(.*)$ [NC]
    # RewriteRule ^(.*)$ https://%1/$1 [R=301,L]
</IfModule>

# Configuración específica para robots.txt
<Files "robots.txt">
    Header set Content-Type "text/plain; charset=utf-8"
    Header set Cache-Control "public, max-age=86400"
</Files>

# Configuración para archivos de verificación de Google
<Files "google*.html">
    Header set Content-Type "text/html; charset=utf-8"
    Header set Cache-Control "public, max-age=86400"
</Files>
